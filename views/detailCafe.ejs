<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= cafe.nama %> - Detail Cafe</title>
  
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary: #0d6efd;
      --primary-light: #e8f2ff;
      --secondary: #6c757d;
      --dark: #212529;
      --light: #f8f9fa;
      --border: #e9ecef;
      --shadow: 0 4px 12px rgba(0,0,0,0.08);
      --shadow-hover: 0 8px 24px rgba(0,0,0,0.12);
    }
    
    * {
      font-family: 'Poppins', sans-serif;
    }
    
    body {
      background-color: #f8fafc;
      color: var(--dark);
      padding-bottom: 80px;
    }
    
    .back-btn {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      color: var(--primary);
      text-decoration: none;
      font-weight: 500;
      margin-bottom: 1.5rem;
      transition: all 0.3s ease;
      padding: 8px 16px;
      border-radius: 8px;
      background: white;
      box-shadow: var(--shadow);
    }
    
    .back-btn:hover {
      gap: 8px;
      color: #0b5ed7;
      transform: translateX(-3px);
    }
    
    .detail-container {
      display: flex;
      gap: 2rem;
      margin-bottom: 2rem;
    }
    
    .detail-content {
      flex: 0 0 75%;
      background: white;
      border-radius: 20px;
      padding: 2rem;
      box-shadow: var(--shadow);
      height: fit-content;
    }
    
    .detail-image-container {
      flex: 0 0 25%;
    }
    
    .detail-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 20px;
      box-shadow: var(--shadow);
    }
    
    .detail-title {
      font-weight: 700;
      color: var(--dark);
      margin-bottom: 1rem;
    }
    
    .detail-description {
      color: var(--secondary);
      line-height: 1.7;
      margin-bottom: 1.5rem;
    }
    
    .info-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1rem;
      margin-top: 1.5rem;
    }
    
    .info-item {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 1rem;
      background: var(--primary-light);
      border-radius: 12px;
    }
    
    .info-item i {
      color: var(--primary);
      font-size: 1.2rem;
      margin-top: 2px;
    }
    
    .info-content h6 {
      font-weight: 600;
      margin-bottom: 4px;
      color: var(--dark);
    }
    
    .info-content p {
      margin: 0;
      color: var(--secondary);
      font-size: 0.9rem;
    }
    
    .map-container {
      border-radius: 16px;
      overflow: hidden;
      margin: 2rem 0;
      box-shadow: var(--shadow);
    }
    
    .map-frame {
      width: 100%;
      height: 400px;
      border: none;
    }
    
    @media (max-width: 768px) {
      .detail-container {
        flex-direction: column-reverse;
        gap: 1.5rem;
      }
      
      .detail-content,
      .detail-image-container {
        flex: 0 0 100%;
      }
      
      .detail-content {
        padding: 1.5rem;
      }
      
      .detail-image {
        height: 300px;
      }
      
      .info-grid {
        grid-template-columns: 1fr;
      }
      
      .map-frame {
        height: 300px;
      }
    }
  </style>
</head>

<body>
<div class="container py-4">
  <a href="/usatour/listcafe" class="back-btn">
    <i class="bi bi-arrow-left"></i> Kembali ke Daftar Cafe
  </a>

  <div class="detail-container">
    <!-- Detail Content (Kiri) -->
    <div class="detail-content">
      <h1 class="detail-title"><%= cafe.nama %></h1>
      <p class="detail-description"><%= cafe.deskripsi %></p>
      
      <div class="info-grid">
        <div class="info-item">
          <i class="bi bi-geo-alt-fill"></i>
          <div class="info-content">
            <h6>Alamat</h6>
            <p><%= cafe.alamat %></p>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Foto (Kanan) -->
    <div class="detail-image-container">
      <img src="<%= cafe.foto %>" class="detail-image" alt="<%= cafe.nama %>">
    </div>
  </div>

  <div class="map-container">
    <h5 class="my-3 fw-semibold text-center">Lokasi di Peta</h5>
    <iframe
      class="map-frame"
      src="https://www.google.com/maps?q=<%= encodeURIComponent(cafe.alamat) %>&output=embed"
      allowfullscreen>
    </iframe>
  </div>
</div>

<%- include('partials/taskbar') %>

<script>
  // Active state for navigation
  document.addEventListener('DOMContentLoaded', function() {
    const currentPage = window.location.pathname;
    const taskItems = document.querySelectorAll('.task-item');
    
    taskItems.forEach(item => {
      if (item.getAttribute('href').includes('/usatour/listcafe')) {
        item.classList.add('active');
      }
    });
  });
</script>
</body>
</html>